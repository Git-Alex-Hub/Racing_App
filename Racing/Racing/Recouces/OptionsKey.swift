import Foundation
import UIKit

enum UserOptionsKey: String {
    case carKey = "key_for_car"
    case carImageKey = "key_for_car_image"
    
    case neonKey = "key_for_neon"
    case neonLocKey = "ket_for_loc_neon"
    
    case speedKey = "key_for_speed"
    case speedLocKey = "key_for_loc_speed"
    
    case scoreKey = "key_for_score"
    case dateKey = "key_for_date"
}
